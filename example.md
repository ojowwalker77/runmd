# runmd Test Document

Some regular text with **bold** and *italic* formatting.

## Shell Commands

List the current directory:

```bash
ll
```

Print a greeting:

```sh
echo "Hello from runmd! 🚀"
```

Show system info:

```bash
pwd
```

## Environment Variables

Project: **runmd** v0.1.0

```bash
echo "Project: $PROJECT_NAME v$VERSION"
```

## Fetch Weather

Get the current weather right from your terminal:

```bash
curl -s wttr.in/?format="%l:+%c+%t+%w"
```

## Non-executable Code

This JavaScript block should render normally without a play button:

```javascript
const greeting = "Hello, World!"
console.log(greeting)
```

## More Formatting

> This is a blockquote that tests rendering of different markdown elements.

Here's a list:
- First item
- Second item with `inline code`
- Third item

### Links

Check out [OpenTUI](https://opentui.com) for the terminal UI framework.

---

That's it! Try pressing **Tab** to navigate between shell blocks and **Enter** to run them.
